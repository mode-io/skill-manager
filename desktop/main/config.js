export const APP_NAME = "Skill Manager";

export const MAIN_WINDOW_BOUNDS = Object.freeze({
  width: 1800,
  height: 1160,
  minWidth: 1200,
  minHeight: 760,
});

export const BACKEND_STARTUP_TIMEOUT_MS = 15000;
export const BACKEND_FORCE_KILL_AFTER_MS = 3000;
